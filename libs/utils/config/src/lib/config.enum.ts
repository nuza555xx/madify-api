export enum APIPrefix {
  AUTHENTICATION = "authentication",
  USER = "user",
  VEHICLE = "vehicle",
}

export enum ConfigKey {
  ELASTICSEARCH = "elasticsearch",
  STORE = "redis-store",
  CACHE = "redis-cache",
  JWT = "jwt",
  MONGOOSE = "mongoose",
  THROTTLER = "throttler",
  STORAGE = "storage",
}
